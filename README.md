Just add your path to image and run the file. The captions will be generated

For venv
# On Windows (PowerShell)
\.venv\Scripts\activate

python blip.py


For virtula image reasoning
#Add image path to img_url
